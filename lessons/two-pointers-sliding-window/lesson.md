# 투 포인터와 슬라이딩 윈도우

투 포인터는 배열이나 문자열에서 두 위치를 움직이며 필요한 구간이나 쌍을 찾는 방법입니다. 모든 쌍을 `O(n^2)`으로 보지 않고, 포인터가 한 방향으로만 움직이게 만들어 `O(n)` 또는 `O(n log n)`으로 줄이는 것이 핵심입니다.

## 0. 선수 지식과 이어지는 레슨

- 선수 지식: 정렬, 누적합, 배열 인덱스 처리
- 함께 보면 좋은 레슨: 누적합과 차분 배열, 이분 탐색
- 다음에 볼 레슨: 좌표 압축, Fenwick Tree, Sqrt Decomposition

## 1. 언제 필요한가

아래 신호가 보이면 투 포인터를 먼저 의심합니다.

- 정렬된 배열에서 합이 특정 값이 되는 두 수를 찾는다.
- 연속 구간의 합, 길이, 종류 수를 묻는다.
- 오른쪽 끝을 늘리면 조건이 좋아지거나 나빠지는 방향이 일정하다.
- 같은 원소를 여러 번 세지 않으면서 모든 후보 구간을 훑어야 한다.

핵심은 **한 포인터가 되돌아가지 않아도 되는가**입니다. 왼쪽 포인터와 오른쪽 포인터가 각각 최대 `n`번만 움직이면 전체 시간은 `O(n)`입니다.

## 2. 정렬된 배열에서 양끝 포인터

정렬된 배열에서 두 수의 합을 확인할 때는 왼쪽 끝과 오른쪽 끝에서 시작합니다.

```text
a[l] + a[r] < target 이면 l을 오른쪽으로 이동
a[l] + a[r] > target 이면 r을 왼쪽으로 이동
```

정렬되어 있기 때문에 `l`을 오른쪽으로 옮기면 합은 커지고, `r`을 왼쪽으로 옮기면 합은 작아집니다.

```cpp
bool hasPairWithSum(vector<int> a, int target) {
    sort(a.begin(), a.end());
    int l = 0;
    int r = (int)a.size() - 1;
    while (l < r) {
        long long sum = (long long)a[l] + a[r];
        if (sum == target) return true;
        if (sum < target) l++;
        else r--;
    }
    return false;
}
```

## 3. 조건을 만족하는 가장 짧은 구간

모든 값이 양수라면 오른쪽 끝을 늘릴수록 구간 합은 커지고, 왼쪽 끝을 줄일수록 구간 합은 작아집니다. 이 단조성 덕분에 합이 `target` 이상인 가장 짧은 연속 구간을 `O(n)`에 찾을 수 있습니다.

```cpp
int minLengthAtLeastSum(const vector<int>& a, long long target) {
    int n = (int)a.size();
    int answer = n + 1;
    int left = 0;
    long long sum = 0;

    for (int right = 0; right < n; right++) {
        sum += a[right];
        while (left <= right && sum >= target) {
            answer = min(answer, right - left + 1);
            sum -= a[left];
            left++;
        }
    }

    return answer == n + 1 ? -1 : answer;
}
```

값에 음수가 섞이면 오른쪽을 늘렸을 때 합이 항상 커지지 않습니다. 그때는 누적합 + 자료구조, prefix minimum, deque 같은 다른 도구가 필요할 수 있습니다.

## 4. 조건을 만족하는 가장 긴 구간

구간 안의 서로 다른 값 개수, 최대 빈도, 합의 상한처럼 "오른쪽을 늘리면 조건이 깨질 수 있고, 왼쪽을 줄이면 회복된다"는 형태도 자주 나옵니다.

```cpp
int longestAtMostKDistinct(const vector<int>& a, int k) {
    unordered_map<int, int> freq;
    int left = 0;
    int answer = 0;

    for (int right = 0; right < (int)a.size(); right++) {
        freq[a[right]]++;
        while ((int)freq.size() > k) {
            int value = a[left++];
            if (--freq[value] == 0) freq.erase(value);
        }
        answer = max(answer, right - left + 1);
    }

    return answer;
}
```

`while` 조건에는 "현재 창이 유효하지 않은 동안"을 넣습니다. 유효해진 뒤에 답을 갱신하면 창이 항상 문제 조건을 만족합니다.

## 5. 슬라이딩 윈도우와 빈도 관리

슬라이딩 윈도우는 보통 연속 구간을 유지합니다. 오른쪽으로 하나 들어오면 빈도를 늘리고, 왼쪽에서 하나 빠지면 빈도를 줄입니다.

```cpp
vector<int> countOnesInEveryWindow(const vector<int>& a, int k) {
    vector<int> result;
    int ones = 0;
    for (int i = 0; i < (int)a.size(); i++) {
        if (a[i] == 1) ones++;
        if (i >= k && a[i - k] == 1) ones--;
        if (i + 1 >= k) result.push_back(ones);
    }
    return result;
}
```

고정 길이 창은 왼쪽과 오른쪽이 동시에 움직입니다. 가변 길이 창은 오른쪽을 먼저 늘린 뒤 조건이 깨지는 동안 왼쪽을 움직입니다.

## 6. 누적합/이분 탐색과 비교

투 포인터가 항상 정답은 아닙니다.

| 상황 | 더 자연스러운 도구 |
| --- | --- |
| 구간 합 질의만 많고 배열은 고정 | 누적합 |
| 답 후보가 단조성을 가진다 | 이분 탐색 |
| 온라인 업데이트와 질의가 섞인다 | Fenwick Tree, Segment Tree |
| 구간 조건이 포인터 이동으로 회복된다 | 투 포인터, 슬라이딩 윈도우 |

투 포인터는 모든 후보를 "정렬된 순서"나 "연속 구간"으로 훑을 수 있을 때 강합니다.

## 7. 자주 하는 실수

| 실수 | 결과 | 점검 |
| --- | --- | --- |
| 음수가 있는데 구간 합 투 포인터를 씀 | 후보를 건너뜀 | 값의 부호 조건 확인 |
| `while` 대신 `if`로 한 번만 줄임 | 창이 여전히 invalid | 조건이 회복될 때까지 줄이기 |
| 답 갱신 위치가 틀림 | 한 칸 짧거나 긴 답 | 창이 valid인 시점에 갱신 |
| 중복 원소 pair count를 대충 셈 | 개수 오답 | 값이 같은 구간을 묶어 계산 |

## 8. 문제를 볼 때 체크할 조건

1. 배열이나 문자열의 순서를 정렬하거나 고정해도 되는가?
2. 포인터 하나가 뒤로 돌아가지 않아도 되는 단조성이 있는가?
3. 구간 조건이 깨졌을 때 왼쪽을 줄이면 다시 회복되는가?
4. 값에 음수가 섞여 단순 합 단조성이 깨지지 않는가?
5. 답이 존재하지 않을 때 반환값을 정했는가?

## 대표 문제로 연결하기

### 문제에서 보이는 신호

- 입력 크기: `n`이 커서 모든 쌍이나 모든 구간을 볼 수 없다.
- 필요한 복잡도: `O(n log n)` 정렬 후 `O(n)` 탐색, 또는 순수 `O(n)`.
- 이 레슨의 핵심 개념: 포인터가 한 방향으로만 움직이도록 조건을 정리한다.

### 풀이 흐름

1. 정렬이 필요한 pair 문제인지, 원래 순서의 연속 구간 문제인지 구분한다.
2. 오른쪽 포인터를 늘렸을 때 조건이 어떻게 변하는지 적는다.
3. 조건이 깨졌을 때 왼쪽 포인터를 몇 번 줄여야 하는지 `while`로 구현한다.

### 자주 틀리는 지점

- 구간 합 문제에서 음수 입력을 놓치면 투 포인터의 단조성이 사라집니다.
- "정확히 K개"와 "최대 K개"는 답 갱신 조건이 다릅니다.

## 9. 연습 문제

| 단계 | 문제 | 목표 |
| --- | --- | --- |
| 입문 | 두 수의 합 찾기 | 정렬 후 양끝 포인터를 그대로 적용 |
| 표준 | 합이 S 이상인 가장 짧은 양수 구간 | `while (sum >= S)` 위치 익히기 |
| 응용 | 서로 다른 값이 K개 이하인 가장 긴 구간 | 빈도 map과 가변 길이 창 결합 |
| 함정 | 음수가 섞인 구간 합 | 투 포인터가 깨지는 조건 확인 |
