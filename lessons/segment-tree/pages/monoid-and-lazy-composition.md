# Segment Tree: Monoid와 Lazy 합성

## 12. 합 말고 다른 연산일 때

Segment Tree에서 바뀌는 것은 세 가지입니다.

1. 두 자식 값을 합치는 `merge`
2. 구간 밖을 만났을 때 돌려줄 항등원
3. 업데이트가 노드 값과 lazy 값에 미치는 영향

| 질의 | merge | 항등원 |
| --- | --- | --- |
| 구간 합 | `a + b` | `0` |
| 구간 최솟값 | `min(a, b)` | 충분히 큰 `INF` |
| 구간 최댓값 | `max(a, b)` | 충분히 작은 `-INF` |
| 구간 gcd | `gcd(a, b)` | `0` |

이처럼 결합 법칙이 성립하고 항등원이 있는 연산을 monoid로 볼 수 있습니다. Segment Tree는 사실상 "구간을 나눠 monoid 값을 합치는 자료구조"입니다.

lazy propagation에서는 lazy 값끼리 어떻게 합쳐지는지도 따로 정해야 합니다.

| 업데이트 | 노드 값 변화 | lazy 합성 |
| --- | --- | --- |
| 구간 덧셈 + 구간 합 | `tree += value * length` | 기존 lazy에 더함 |
| 구간 덧셈 + 구간 최솟값 | `tree += value` | 기존 lazy에 더함 |
| 구간 대입 + 구간 합 | `tree = value * length` | 이전 lazy를 새 대입 값으로 덮음 |
| 구간 대입 + 구간 최솟값 | `tree = value` | 이전 lazy를 새 대입 값으로 덮음 |

구간 덧셈과 구간 대입이 동시에 있으면 "대입 뒤 덧셈"과 "덧셈 뒤 대입"의 순서가 결과를 바꿉니다. 이 경우 lazy 상태를 단일 숫자로 두기보다 `hasAssign`, `assignValue`, `addValue`처럼 의미를 분리해 합성 규칙을 명시하는 편이 안전합니다.

## 13. Bottom-up과 top-down 중 무엇을 쓸까

| 상황 | 추천 |
| --- | --- |
| 점 업데이트 + 구간 합/최솟값 | bottom-up 반복 구현 |
| 설명과 디버깅이 중요하다 | top-down 재귀 구현 |
| lazy 구간 업데이트가 필요하다 | top-down 재귀 구현 |
| 재귀 제한이나 함수 호출 비용이 부담된다 | bottom-up 반복 구현 |

둘 다 같은 Segment Tree입니다. 차이는 노드를 어떻게 번호 붙이고 순회하느냐입니다. 처음 배울 때는 top-down으로 구간 관계를 이해하고, 익숙해진 뒤 bottom-up 구현을 쓰면 좋습니다.

## 14. 시간 복잡도

| 작업 | 시간 |
| --- | --- |
| 빌드 | `O(n)` |
| 점 업데이트 | `O(log n)` |
| 구간 질의 | `O(log n)` |
| lazy 구간 업데이트 | `O(log n)` |
| 메모리 | `O(n)` |

`4 * n` 재귀 구현은 실제 메모리를 네 배 가까이 잡지만 복잡도 표기상은 `O(n)`입니다. bottom-up 구현은 보통 `2 * n` 정도를 사용합니다.

Segment Tree는 구간을 작은 조각으로 분해해 필요한 조각만 합치는 도구입니다. 저장 값의 결합 규칙과 lazy를 합치는 규칙만 정확히 정하면 합, 최솟값, 최댓값, gcd 같은 많은 구간 문제에 적용할 수 있습니다.

## 15. 자주 하는 실수

| 실수 | 결과 | 확인 방법 |
| --- | --- | --- |
| 구간 밖 항등원을 잘못 반환 | 최솟값/최댓값 질의가 오염됨 | 합은 `0`, 최솟값은 큰 `INF`, 최댓값은 작은 `-INF` |
| lazy 값을 자식에게 내리지 않음 | 일부 구간 질의가 오래된 값을 봄 | 방문 직후 `push` 위치를 고정 |
| 구간 덧셈과 대입을 같은 방식으로 합침 | 업데이트 순서가 바뀌어 오답 | `hasAssign`, `assignValue`, `addValue`처럼 의미 분리 |
| `int`로 구간 합 저장 | 큰 입력에서 overflow | 노드 값과 lazy 값은 `long long` 검토 |

## 16. 문제를 볼 때 체크할 조건

1. 배열 값이 중간에 바뀌는가?
2. 구간 합, 최솟값, 최댓값, gcd처럼 결합 가능한 질의인가?
3. 점 업데이트인지 구간 업데이트인지 구분했는가?
4. 구간 업데이트라면 lazy 합성 규칙이 덧셈인지 대입인지 확인했는가?
5. Sqrt Decomposition이나 Fenwick Tree로 더 단순하게 풀 수 있는가?

## 17. 연습 문제

| 단계 | 문제 | 목표 | 힌트 키워드 |
| --- | --- | --- | --- |
| 입문 | TODO: 점 업데이트 + 구간 합 문제 추가 | 기본 Segment Tree의 `build`, `query`, `update` 구현 | 점 업데이트, 구간 합 |
| 표준 | [창고 구역 장부](/practice/SHELFLOG) | 구간 덧셈과 구간 합 질의를 lazy로 처리 | lazy propagation, `long long` |
| 응용 | TODO: 구간 최솟값 + 구간 덧셈 문제 추가 | 합이 아닌 monoid에서 lazy 값 적용 | 항등원, `min` merge |
| 함정 | TODO: 구간 대입과 구간 덧셈이 섞인 문제 추가 | lazy 합성 순서와 덮어쓰기 처리 | `hasAssign`, 합성 순서 |
